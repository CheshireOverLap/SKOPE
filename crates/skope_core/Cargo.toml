[package]
name = "skope_core"
version = "0.1.0"
edition = "2021"
authors = ["SKOPE Games <admin@skope.games>"]
description = "Core ECS components and resources for SKOPE Engine"
license = "MIT OR Apache-2.0"

[dependencies]
bevy_ecs = { workspace = true }
glam = { workspace = true }
bytemuck = { workspace = true }
wgpu = { workspace = true }
winit = { workspace = true }

[dev-dependencies]
# For tests

[features]
default = []
