[package]
name = "skope_shading"
version = "0.1.0"
edition = "2021"
authors = ["SKOPE Games <admin@skope.games>"]
description = "Character shading systems (Skin SSS, Eye, Face) for SKOPE Engine"
license = "MIT OR Apache-2.0"

[dependencies]
glam = { workspace = true }
bytemuck = { workspace = true }
wgpu = { workspace = true, optional = true }

[features]
default = []
# Enable GPU resource creation functions
gpu = ["wgpu"]
